nelmio_api_doc:
    documentation:
        info:
            title: Test case E-sites
            description: Basic symfony project to execute the asignment
            version: 1.0.0
    areas: # to filter documented areas
        path_patterns:
            - ^/api(?!/doc$) # Accepts routes under /api except /api/doc
